{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build AOCODARCH7DUAL",
            "type": "shell",
            "command": "make AOCODARCH7DUAL",
            "group": "build",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        },
        {
            "label": "Build AOCODARCH7DUAL",
            "type": "shell",
            "command": "make AOCODARCH7DUAL",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
        ,
        {
            "label": "CMAKE Update",
            "type": "shell",
            "command": "cmake -G Ninja .. -DVERSION_TYPE=DEV",
            "group": "build",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        },
        {
            "label": "CMAKE Update Debug",
            "type": "shell",
            "command": "cmake -G Ninja .. -DCMAKE_BUILD_TYPE=Debug -DVERSION_TYPE=DEV",
            "group": "build",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        },
        {
            "label": "Build BETAFPVF405 debug",
            "type": "shell",
            "command": "cmake --build . --target BETAFPVF405 --config Debug",
            "dependsOn": [
                "CMAKE Update Debug"
            ],
            "dependsOrder": "sequence",
            "group": "build",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ]
}
